package com.apexinnovators.entity;

/** Community post type (posts.type ENUM). */
public enum PostType {
    DISCUSSION,
    PROJECT,
    ACHIEVEMENT,
    HACKATHON,
    RESOURCE,
    QUESTION,
    ANNOUNCEMENT
}
